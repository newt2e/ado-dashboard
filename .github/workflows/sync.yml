name: Update Dashboard

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Get ADO Data
        run: |
          curl -u :${{ secrets.ADO_PAT }} "curl -u :${{ secrets.ADO_PAT }} "https://dev.azure.com/SmartQ-Global-Digital-Cafeteria-Suite/Gartner%20-%20SQ%20Transformation/_apis/wit/workitems?ids=1&api-version=7.1" -o data/dashboard.json" -o data/dashboard.json

      - name: Commit Changes
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add .
          git commit -m "update data" || echo "No changes"
          git push
